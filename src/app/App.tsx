import React from 'react';
import '../styles.scss';
import {Layout} from "app/ui/layout/layout";
import {Router} from "routing";
function App() {
    return (
        <Layout>
            <Router/>
        </Layout>

    );
}

export default App;
